name: "ilo"
version: "1.0"
config-version: 2

profile: clickhouse_ftw

model-paths: ["models"]
macro-paths: ["macros"]

models:
  ilo:
    +schema: ilo
    clean:
      +schema: clean
      +materialized: table
    mart:
      +schema: mart
      +materialized: view
